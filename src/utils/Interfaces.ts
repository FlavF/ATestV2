export interface IDatas {
  lat: string;
  lon: string;
  event_type?: string;
}

export interface IDescription {
  lat: number;
  lon: number;
  name: string;
  impressions: number;
  clicks: number;
}